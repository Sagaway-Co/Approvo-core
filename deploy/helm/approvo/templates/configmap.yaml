apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "approvo.fullname" . }}-config
  labels:
    {{- include "approvo.labels" . | nindent 4 }}
data:
  config.yaml: |
{{ toYaml .Values.config | indent 4 }}
