apiVersion: v1
kind: Service
metadata:
  name: {{ include "approvo.fullname" . }}
  labels:
    {{- include "approvo.labels" . | nindent 4 }}
spec:
  type: ClusterIP
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "approvo.selectorLabels" . | nindent 4 }}
