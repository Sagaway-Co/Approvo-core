# 给 AI 助手的须知（CLAUDE.md）

## 🚨 这是一个【对公众开放】的仓库

本仓库是 PUBLIC 的：任何人可读、可爬取、可 fork。**推送到这里的内容无法真正撤回。**

**因此，绝对不要在本仓库的任何位置写入：**

| 类别 | 例子 |
|---|---|
| 凭据 | 口令、密码、token、AccessKey/Secret、证书、证书链、私钥、kubeconfig |
| 网络与拓扑 | 内网 IP、内网域名、集群名、namespace、镜像仓库实例地址、数据库地址 |
| 身份 | 真实人名、手机号、邮箱、IM 平台的 app_id / app_secret / open_id / chat_id / 审批定义码 |
| 组织信息 | 客户名、租户标识、内部仓库名、内部服务清单、内部发版链路细节 |

**"任何位置"包括**：代码、注释、文档、示例、配置、测试固件、提交信息、分支名、Issue 与 PR 描述。

## 需要举例时怎么写

一律使用中性占位符：`my-app` / `my-org` / `prod-cluster` / `registry.example.com` /
`https://approvo.example.com` / `cli_xxxxxxxxxxxx`。
配置项一律走环境变量或配置文件，**不要给出真实的默认值**（尤其不要让默认值指向某个生产环境）。

## 机械兜底与它的边界

CI 里有 `leak-guard` job，用**模式匹配**拦截 AccessKey、私钥、JWT、平台 token、真实 IM 标识、内网 IP。
它是安全网，不是许可证：

⚠️ **它只能拦「已知模式」，拦不住「语义上的敏感信息」** ——
客户名、内部拓扑描述、事故细节、业务背景，这些没有固定形状，门禁看不出来。**那部分只能靠人判断。**

## 🔴 铁律：内部交接文档绝不进本仓

维护方内部有 `HANDOFF` 类文档（交接 / 事故复盘 / 里程碑），记录着具体环境、事故细节与人员信息。
**这类文档一个字都不得进入本仓**，也**不存在"脱敏之后放进来"这个选项** ——
它们的价值恰恰来自那些具体细节，脱敏到可公开就已经没有价值了。

需要让外部读者了解的内容，请**从零写**面向使用者的文档（README / `docs/`），不要从内部文档改写。
CI 的 `leak-guard` 会按文件名拦截这类文件，但它拦不住"把内容粘进别的文件"——那要靠你。

## 如果你不确定

**停下来问，而不是猜。** 把一个字符串写进公开仓库是不可逆的；多问一句是免费的。
